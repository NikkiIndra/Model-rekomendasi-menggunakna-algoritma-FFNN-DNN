[tool.poetry]
name = "mindcare-api"
version = "1.0.0"
description = "MindCare Stress & Activity Recommendation API"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
